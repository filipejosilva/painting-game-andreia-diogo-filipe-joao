import org.academiadecodigo.simplegraphics.graphics.Color;
import org.academiadecodigo.simplegraphics.keyboard.Keyboard;

public class PlayerFactory {

    /*public static Player.Player.HumanPlayer getNewHumanPlayer(Background background, Color color, int[] coordinates) {
        Player.Player.HumanPlayer humanPlayer;
        humanPlayer = new Player.Player.HumanPlayer(background, color, coordinates);
        return humanPlayer;
    }

    public static Player.Player.HumanPlayer getNewAIPlayer(Background background, Color color, int[] coordinates) {
        Player.Player.HumanPlayer AIplayer;
        AIplayer = new AIPlayer(background, color, coordinates);
        return AIplayer;
    }*/
}
